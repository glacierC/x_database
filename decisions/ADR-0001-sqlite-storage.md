# ADR-0001 - 使用 SQLite 作为存储层

## 背景
需要一个持久化存储来保存抓取的推文，支持去重和增量查询。

## 决策
使用 SQLite（本地文件），不用 PostgreSQL 或其他网络数据库。

## 原因
- 单机部署，无并发写入压力（只有一个 scraper 进程）
- 下游消费方（分析项目）直接读文件即可，零配置
- 备份简单：cp 一个文件
- 推文数据量级（每账号每天几十条）不需要网络数据库

## 代价
- 不支持多机共享
- 高并发读写性能差（当前场景不涉及）

## 日期
2026-02-26
